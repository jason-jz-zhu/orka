# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust build artifacts
target/
src-tauri/target/
Cargo.lock.bak
**/*.rs.bk

# Playwright E2E run outputs (per-run screenshots, HTML report).
# Regenerated on every `npm run test:e2e`; nothing in here should ship.
test-results/
playwright-report/

# Tauri
src-tauri/gen/

# OS / misc
.env
.env.local
*.pem

# Claude Code local state (per-dev, not code)
.claude/

# Private docs (not published — internal strategy, sensitive notes)
docs/_private/

# Icon build artifacts (intermediate iconset used by iconutil)
assets/_icon_tmp/

# Demo video build artifacts (committed finals only)
demo-output/orka-demo-*.mp4
demo-output/typing-sound.wav
demo-output/short-cuts/


# Demo build artifacts (keep only final + scripts)
demo-output/*.webm
demo-output/*.aac
demo-output/*-silent.mp4
demo-output/v[0-9]*
demo-output/cl*
demo-output/clean-*
demo-output/synced-*
demo-output/final-*
demo-output/voice/
demo-output/voice-*/
demo-output/voice-clean/
demo-output/frames/
demo-output/timing/
demo-output/voiceover-*
